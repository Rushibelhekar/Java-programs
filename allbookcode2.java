

import java.util.*;

public class allbookcode2 {
      public static void main(String[] args) {

            // program to display array element in descending order
            /*
             * Scanner sc = new Scanner(System.in);
             * System.out.println("enter the limit :");
             * int n = sc.nextInt();
             * int[] array = new int[10];
             * System.out.println("enter the array elements:");
             * for (int i = 0; i < n; i++) {
             * array[i] = sc.nextInt();
             * }
             * System.out.println("the array elements are:");
             * for (int i = 0; i < n; i++) {
             * System.out.println("arr[" + i + "]=" + array[i]);
             * }
             * for (int i = 0; i < n; i++) {
             * for (int j = i + 1; j < n; j++) {
             * if (array[i] < array[j]) {
             * int temp = array[i];
             * array[i] = array[j];
             * array[j] = temp;
             * 
             * }
             * }
             * }
             * System.out.println("the sorted array is:");
             * for (int i = 0; i < n; i++) {
             * System.out.println(array[i]);
             * }
             * sc.close();
             */

            // program to display array elements
            /*
             * Scanner sc = new Scanner(System.in);
             * System.out.println("enter the limit :");
             * int n = sc.nextInt();
             * int array[] = new int[10];
             * System.out.println("enter the array elements:");
             * for (int i = 0; i < n; i++) {
             * array[i] = sc.nextInt();
             * }
             * System.out.println("array elements are:");
             * for (int i = 0; i < n; i++) {
             * System.out.println("arr[" + i + "]=" + array[i]);
             * }
             * sc.close();
             */

            // program to get even or odd num from array
            /*
             * Scanner sc=new Scanner(System.in);
             * System.out.println("enter the limit of array:");
             * int n=sc.nextInt();
             * int[] array=new int[10];
             * System.out.println("enter the array elments:");
             * for(int i=0;i<n;i++)
             * {
             * array[i]=sc.nextInt();
             * }
             * System.out.println("even numbers are:");
             * for(int i=0;i<n;i++)
             * {
             * if(array[i]%2==0)
             * {
             * System.out.println(array[i]);
             * }
             * }
             * System.out.println("odd numbers are:");
             * for(int i=0;i<n;i++)
             * {
             * if(array[i]%2!=0)
             * {
             * System.out.println(array[i]);
             * }
             * }
             * sc.close();
             */

            // program for two dimentional arry
            /*
             * Scanner sc = new Scanner(System.in);
             * System.out.println("enter the number of rows:");
             * int row = sc.nextInt();
             * System.out.println("enter the number of the column:");
             * int col = sc.nextInt();
             * int array[][] = new int[row][col];
             * System.out.println("enter the number of the elements :");
             * for (int i = 0; i < row; i++) {
             * for (int j = 0; j < col; j++) {
             * array[i][j] = sc.nextInt();
             * }
             * }
             * System.out.println("the array is: ");
             * for (int i = 0; i < row; i++) {
             * for (int j = 0; j < col; j++) {
             * System.out.print(array[i][j] + "\t");
             * }
             * System.out.print("\n");
             * }
             */

            

      }

}
